{
  "name": "spectify-monorepo",
  "private": true,
  "version": "1.0.0-rc.5",
  "description": "Monorepo for the linting orchestrator, the linting reporting service, and the linting document store.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/cisco-open/linting-orchestrator.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@cisco_open/linting-document-store && npm run build --workspace=@cisco_open/linting-reports && npm run build --workspace=@cisco_open/linting-orchestrator",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev": "npm run dev --workspace=@cisco_open/linting-orchestrator",
    "add-ruleset": "npm run add-ruleset --workspace=@cisco_open/linting-orchestrator",
    "install-rulesets": "npm run install-rulesets --workspace=@cisco_open/linting-orchestrator",
    "check-rulesets": "npm run check-rulesets --workspace=@cisco_open/linting-orchestrator",
    "generate-openapi": "npm run generate-openapi --workspace=@cisco_open/linting-orchestrator",
    "sync-badge": "node scripts/sync-badge.mjs",
    "prerelease": "npm run sync-badge && npm run build && npm test",
    "postinstall": "npm run install-rulesets"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "@stoplight/spectral-rulesets": "1.22.2"
  }
}